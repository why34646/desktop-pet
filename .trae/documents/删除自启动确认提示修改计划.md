# 删除自启动确认提示修改计划

## 一、需求概述
删除勾选和取消勾选开机自启动时的确认提示，让复选框可以直接切换状态。

## 二、修改方案

### 修改文件：project/main.py

#### 修改 toggle_autostart() 方法
删除所有确认提示代码，保留空方法或直接不做任何处理：

```python
def toggle_autostart(self, state):
    pass
```

或者更彻底地，直接移除这个方法的内容，因为现在不需要处理任何确认逻辑。
